[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "OpenCFD-AMR"
authors = [{name = "David Song", email = "mylab_octave@163.com"}]
dynamic = ["version", "description"]
